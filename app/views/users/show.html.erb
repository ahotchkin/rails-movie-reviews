<h1>Hello, <%= current_user.username %></h1>

<% if current_user.admin %>
  <h3>ADMIN</h3>
  <br>
<% end %>

<h2>Here are the latest movie reviews:</h2>

<%= render partial: "reviews/reviews", :locals => {:reviews => @reviews, :movie => @movie} %>
